# Squishy Pals · Agent 工作入口

## 按任务读取

- 产品范围、用户流程、参数与恢复语义：[PRODUCT](docs/PRODUCT.md)。
- 开发调试、模块接口、扩展角色、验证命令：[ARCHITECTURE](docs/ARCHITECTURE.md)。开始开发或调试前读取其中的环境说明。
- 核心流程、共享机制或质量验收：[QUALITY-GATES](docs/QUALITY-GATES.md)。
- 界面或角色外观任务：先查架构文档的「样式与参考输入」，按本轮范围读取实际参考。
- 只有存在有接续价值的 `session-handoff.md` 时读取恢复点；普通运行状态通过实时工具查询。

低风险小改只读目标位置和直接事实源。同一会话已读且未变的材料按需复用，不机械通读全部文档。

## 执行

- 以用户当前交付物和后续纠偏为准；要求依据材料执行时，阅读不能替代交付。用户最新明确要求与本文档冲突时，按最新要求执行，只同步由此形成的持久契约。
- 非琐碎任务先简述目标、可验证成功标准、影响范围、假设、主要风险、最小方案及「步骤 → 验证」；明确的小改可压缩为一句目标和一个检查。
- 能从仓库和环境核实的事实先自行确认。低风险、可逆且不改变关键结果的缺口，说明假设后继续；只在实质歧义、关键风险或真实阻塞时澄清，并先完成独立部分。
- 编辑前读取目标文件、直接调用方和共享工具。选择最小充分方案，复用已有能力；不添加未要求的功能、抽象、配置或无证据的错误处理，不重构或格式化无关内容。
- 保留已有未提交改动；只清理由本次改动产生的孤儿内容。预存死代码或无关问题只报告。
- 共享输入、接口或生命周期发生问题时，检查同一数据流的消费端和相邻场景，避免仅修当前样例；没有新证据时不扩大审计。
- 完成已授权的实现、必要运行和对应检查，修复本次引入的问题。检查通过后不因惯性扩大验收；失败和未验证项如实说明。
- 破坏性命令、数据删除、生产迁移或外部提交仅在已有明确授权时执行，不重复请求已获授权动作的确认。凭据不写入文档、代码或日志。

## 文档职责与维护

- `PRODUCT` 是产品语义的权威位置；`ARCHITECTURE` 是工程机制和命令的权威位置；`QUALITY-GATES` 是跨功能验收政策的权威位置。本文件只保留工作入口与规则。
- 只有作用域已确认、未来仍影响决策的契约变化才更新长期文档。局部修复、实现细节和一次性验证优先留在代码、测试和任务结果，不为留痕重复写文档，也不改文档为已知错误背书。
- Harness 不创建、修改或审计设计规则、品牌 CSS、Design DNA 或设计系统；这些产物的建立或修改交由 `leo-design-md`，本项目 Harness 只记录实际工程接入与证据要求。
- 仅当事项尚未闭环、遗漏会改变下一安全动作或完成判断、无法从代码/版本控制/任务结果/实时工具低成本恢复、且有准确停止点与下一动作时，才创建或更新 `session-handoff.md`。只保留当前接续单元和删除条件，闭环后删除；不存历史流水、通用待办、PID、端口或测试通过记录。

## 子代理、模型与沟通

- 边界清楚且委派能节省时间或上下文的任务优先使用子代理；仅在任务独立时并行。明确文件所有权、最小成功标准和真实阻塞条件，告知代理保留并适配他人改动。
- 保留用户当前模型与推理设置。工具允许选择新任务或子代理参数时，简单查找用 `low`、常规实现用 `medium`、多文件权衡或难复现问题用 `high`；只有用户明确要求或 `high` 有可说明的不足时使用更高强度。
- 默认简体中文，优先事实和可验证证据。采用外部分析视角，简洁报告结果、相关检查和真实限制；不过度拟人化，不用流程叙述替代工作。

## 完成标准

目标已交付、差异只服务本轮范围、适用检查有证据、本次引入的问题已修复。按质量门禁区分通过、失败、未验证与不适用；核心任务不能只凭类型检查、构建或数值断言声明体验完成。仅同步实际变化的长期契约及符合准入的恢复点。
