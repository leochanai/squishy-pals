# Squishy Pals · 掌心游乐场

本文件描述当前实际保留的首页、伙伴图鉴和角色游乐场设计。它不是完整验收证明；逐页评分、剩余问题与检查证据见 [改造记录](docs/palm-playground/REPORT.md)。

## 方向与页面分工

采用用户选定的 A「掌心游乐场」：奶油底、圆润玩具、清楚的直接操作。首页以白色厚手套拉扯八爪鱼的插画建立玩法印象；图鉴以九张完整角色肖像帮助识别与选择；游乐场以真实 WebGPU 角色和指针操作为主体，设置退居辅助位置。保留全部角色、颜色、三种材质、饰品、软硬、阻尼、戳动和恢复功能。

首页入口可直接开始八爪鱼，也可进入图鉴；图鉴整张角色卡可进入对应游乐场。游乐场底部选择器可切换伙伴，页头回图鉴或首页。角色状态保留及重置语义以 [PRODUCT](docs/PRODUCT.md) 为准。

## 色彩与字体

- 奶油纸色 `#fffaf0` 为页面底；深绿 `#243025` 为文字与主要按钮。
- 橙色 `#f45b32` 用于品牌、首页和图鉴标题强调及小控件键盘焦点；舞台焦点采用低饱和绿 `#7b925f` 的细线。
- 浅绿 `#e7f3cd` 为游乐场舞台，图鉴肖像使用统一浅绿背景；首页插画使用更鲜明的黄绿背景。
- 品牌使用已有 Squishy Display。首页中文保留系统无衬线；图鉴与角色标题使用本地圆体子集，正文为 Arial / PingFang SC 等系统字体。字体实际覆盖和响应式字号以各页面 CSS 为准。
- 紫、粉、橙、薄荷绿是可选角色色；机械八爪鱼默认金属灰 `#a9afb0`，哑光壳体与深色关节共同表达机械感。手动选色跨材质保留。

## 构图与响应式

首页宽屏为文案与插画双栏，短屏收紧标题和间距，手机顺序为文案、开始按钮、插画与三步玩法。图鉴桌面和平板三列，宽度不超过760px时双列，末项等宽居中；保持角色完整轮廓，名称和玩法置于图片外。

游乐场桌面将大舞台与304px设置列并排；标题、画布、动作和角色选择器各有独立排布空间，不叠压设置或分割线。宽度不超过1000px时设置落到舞台后，平板使用两列设置及独立整行饰品。宽度不超过600px时，默认突出捏玩，设置由「外观与手感」展开；展开后沿用同一画布，以220px固定预览保持角色可见，返回保留选择与焦点。角色条允许水平滚动。

画布取景根据实际容器与角色轮廓计算，横向占比上限为66%，纵向桌面76%、小屏74%，并保障底部余量与移动边界，不能仅放大容器而保持角色很小。桌面、短屏、平板、手机的代表检查尺寸为1580×916、1280×720、834×1112、390×844，并补充320px及断点两侧检查。

## 素材、材质与交互状态

首页保留用户认可的暖白卡通厚手套插画，拉伸方向上的眼睛和脸颊随表面偏移。图鉴使用统一软玩具风格的九张肖像，作为选择入口，不替代真实3D角色。

游乐场悬浮手套采用相机朝向的立体四指造型，弯曲食指与较短拇指形成C形捏合开口，抓取时收拢。软胶为暖白，果冻为乳白透光，机械为暖白壳体加深色指节；切换角色材质同步切换手套材质，角色颜色不染色手套。手套随命中位置出现、离开隐藏，释放后重新检查命中。当前轮廓精细度及抓取中间态仍有复查空间。

角色使用柔和摄影棚光照与接触阴影；八爪鱼软胶表面保留圆润连续性，果冻使用双面半透明壳体、低吸收体积色和厚薄差异，使后侧轮廓能够隐约透出；折射保留明暗细节，主体色相以选色和原有顶点颜色为准，防止绿色舞台覆盖选色，机械采用分节结构。真实变形、回弹和面部跟随由角色求解器驱动。

主要动作使用深绿实底，恢复为浅底轮廓按钮。材质选中有深色底与指示器，颜色和饰品使用轮廓及勾选；不可用控件淡化。手机设置显示返回按钮。图鉴 hover 轻抬升、微转和缩放，尊重 reduced-motion；手套开合与转向使用平滑过渡。动效服务接触、选择和回弹反馈。

## 实现入口与限制

页面样式分别位于 `app/home.module.css`、`app/pals/catalogue.module.css`、`src/core/playground.module.css`；三维材质、手套和灯光由对应渲染模块持有，不能通过 CSS 替代。旧全局规则不是新页面的布局规范。

当前检查不包含真实触屏、全部浏览器兼容和完整抓取中间帧；手机视口的鼠标检查不能替代触控验收。首页系统中文标题、图鉴箭头语义及游乐场素材细节仍有已记录差距。自动生成或改写的文案待人工逐行审稿。不得以停止迭代或构建通过声称全部视觉与功能验收通过。
